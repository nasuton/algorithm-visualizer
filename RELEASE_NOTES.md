# Algorithm Visualizer v0.1.0

C++ / SFMLによる8種類のソート可視化アプリの初回リリースです。

## 主な機能

- マージ、バブル、挿入、選択、クイック、ヒープ、カウント、基数ソート。
- 数字キー1〜8での切り替え（テンキー対応）。
- Spaceで一時停止・再開、Rで最初から再実行。
- 日本語のソート名・説明・処理状況と、棒グラフによる途中経過の表示。
- アルゴリズムと描画を分離したC++17のクラス構成。

## ダウンロードと起動

`algorithm-visualizer-v0.1.0-windows-x64.zip` を展開し、
`algorithm_visualization.exe` を起動してください。
ソースコード一式は `algorithm-visualizer-v0.1.0-source.zip` です。
`SHA256SUMS.txt` で両ZIPのハッシュを確認できます。

Windows x64、日本語フォント（メイリオ等）、OpenGLを利用できるグラフィックス環境が必要です。
必要に応じてMicrosoft公式のVisual C++再頒布可能パッケージ（x64）をインストールしてください。
WindowsのフォントやMSVCランタイムは同梱していません。

## 検証

MSVCのReleaseビルドとCTestを使用。
8,097ケースの整列結果、31ケースの範囲超過の拒否に加え、
途中リセットと1ステップ更新を確認しています。
別PCでのGUI動作と、Windows以外の動作は未検証です。

## 現在の制限

- 入力は固定の10要素（1〜10）、再生間隔は0.25秒です。
- 数字キーによる切り替えは、同じ初期配列から再生し直します。
- ステップ数は各アルゴリズムで定義が異なり、速度のベンチマークではありません。
- カウントソートの値の範囲は100万以下です。
- ヒープの木構造描画、入力編集、速度変更のUIはありません。

第三者ライブラリの表記は、ZIP内の `THIRD_PARTY_NOTICES.md` と `licenses/` を参照してください。
